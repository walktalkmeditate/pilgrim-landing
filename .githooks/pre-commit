#!/bin/sh
# Block commits that desync the generated sunpath pages or break the
# structured-data / metadata graph. Bypass (rarely) with: git commit --no-verify
set -e

node scripts/sunpath/build-permalinks.mjs --check
node scripts/validate-metadata.mjs
